{% include 'email/partial/emailHeader.html.twig' with { recipientFirstName : recipientFirstName } %}

<p>Your request for <strong>{{ needsApprovalByFullName }}</strong> to teach the following lesson topic:
    <strong>{{ lesson.title }}</strong> has been denied.</p>
<p>Login to the <a href="{{ url('dashboard') }}">dashboard</a> to view</p>

{% include 'email/partial/emailFooter.html.twig' %}