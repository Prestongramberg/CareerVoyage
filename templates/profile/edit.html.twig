{% extends 'baseAuthenticated.html.twig' %}

{% form_theme form _self %}

{% block title %}Edit Profile{% endblock %}

{% block _professional_edit_profile_form_photo_row %}

      {% if form.vars.name == 'photo' %}
          {#{% set photo = form.vars.data %}#}
        {#  <div class="row">
          <div class="col-lg-2">
              <div class="form-group">
                  <label class="control-label">Existing Image</label>
                  <div>
                      {% if image and image.id %}
                          <img style="width: 100%; max-width: 100px; height: auto;" src="{{ (image.container ~ "/" ~ image.path) | imagine_filter('my_thumb') }}" class="js-existing-image"/>
                      {% else %}
                          <img style="width: 100%; max-width: 100px; height: auto;" src="" class="js-existing-image"/>
                      {% endif %}
                  </div>
              </div>
          </div>#}

         {# {% if photo.path is defined %}
              <img style="width: 100%; max-width: 100px; height: auto;" src="{{ photo.path }}" />
          {% endif %}#}

              {{ form_widget(form) }}
      {% endif %}
{% endblock %}

{% block body %}


    <h1>Edit Profile</h1>

    {{ form_start(form, {attr: {'novalidate' : 'novalidate'}}) }}

    {{ form_row(form.firstName) }}
    {{ form_row(form.lastName) }}
    {{ form_row(form.photo) }}
    {{ form_row(form.email) }}
    {{ form_row(form.username) }}

    {{ form_row(form.password) }}
    {{ form_row(form.passwordRepeat) }}
    <button class="btn">Update</button>
    {{ form_end(form) }}

{% endblock %}
