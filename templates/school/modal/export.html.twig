<div class="uk-modal-dialog">
    <button class="uk-modal-close-outside" type="button" uk-close></button>
    <div class="uk-modal-header">
        <h4>Export {{ type }}(s) <small>(<span style="font-weight: bold">{{ userCount }}</span> Users Selected)</small></h4>
    </div>

    {{ form_start(form, {attr: {'id': 'js-bulk-action-form', 'novalidate' : 'novalidate'}}) }}

    <div class="uk-modal-body">

        {{ form_start(form, {'attr': {novalidate: 'novalidate' }}) }}

        {% if form_errors(form) %}
            <div class="uk-alert-danger" uk-alert>
                <a class="uk-alert-close" uk-close></a>
                <div>{{ form_errors(form) }}</div>
            </div>
        {% endif %}

        <p class="uk-text-right">
            <button class="uk-button uk-button-default uk-modal-close" type="button">Cancel</button>
            <button class="uk-button uk-button-primary" type="submit">Export</button>
        </p>

        {{ form_rest(form) }}
    </div>

    {{ form_end(form) }}
</div>



