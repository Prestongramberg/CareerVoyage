{% extends 'base.html.twig' %}

{% block title %}Pintex Solutions - Welcome{% endblock %}
{% block bodyClass %}page-login{% endblock %}


{% block body %}
    <div class="uk-flex uk-flex-center uk-height-viewport">
        <div class="uk-container login-box">
            <div class="uk-card uk-card-default uk-card-body">

                <h1 class="uk-card-title">Pintex Solutions<sup>(BETA)</sup></h1>

                <ul class="uk-child-width-expand" data-uk-tab="{connect: '#tab-login'}" uk-switcher>
                    <li class="uk-active"><a href="#login">Login</a></li>
                    <li><a href="#register">Register</a></li>
                </ul>

                <div class="uk-switcher" id="tab-login">
                    <div class="login-box__login">
                        <form method="post">
                            {% if error %}
                                <div class="uk-alert-danger" uk-alert>
                                    <a class="uk-alert-close" uk-close></a>
                                    <p>{{ error.messageKey|trans(error.messageData, 'security') }}</p>
                                </div>
                            {% endif %}
                            <fieldset class="uk-fieldset">
                                <div class="uk-margin">
                                    <input class="uk-input" type="email" value="{{ last_username }}" name="email" id="inputEmail" placeholder="Email" required autofocus>
                                </div>
                                <div class="uk-margin">
                                    <input class="uk-input" type="password" name="password" id="inputPassword" placeholder="Password" required>
                                </div>
                                <div class="uk-margin">
                                    <button class="uk-button uk-button-primary uk-width-1-1">Submit</button>
                                </div>
                            </fieldset>

                            <input type="hidden" name="formType" value="loginForm">
                            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                        </form>

                        <p>Dont have an Account yet? <a href="#register">Register</a></p>
                    </div>
                    <div class="login-box__register">
                        <ul class="uk-flex uk-flex-around" data-uk-tab="{connect: '#tab-register-options'}" uk-switcher>
                            <li class="uk-active"><a href="#register-professional"><i class="fas fa-briefcase"></i>Professional</a></li>
                            <li><a href="#register-educator"><i class="fas fa-graduation-cap"></i>Educator</a></li>
                            <li><a href="#register-student"><i class="fas fa-book-reader"></i>Student</a></li>
                        </ul>

                        <div class="uk-switcher login-box__register-tab-contents" id="tab-register-options">
                            <div>
                                <p>Pintex empowers professionals to impact and positively alter the trajectory of students’ lives by sharing their expertise and career experience.</p>

                                {{ form_start(professionalRegistrationForm, {attr: {'novalidate' : 'novalidate'}}) }}
                                    <fieldset class="uk-fieldset">
                                        <div class="uk-margin">
                                            {{ form_label(professionalRegistrationForm.firstName, 'First Name*') }}
                                            {{ form_widget(professionalRegistrationForm.firstName, {'attr': {'class': 'uk-input'}}) }}
                                        </div>
                                        <div class="uk-margin">
                                            {{ form_label(professionalRegistrationForm.lastName, 'Last Name*') }}
                                            {{ form_widget(professionalRegistrationForm.lastName, {'attr': {'class': 'uk-input'}}) }}
                                        </div>
                                        <div class="uk-margin">
                                            {{ form_label(professionalRegistrationForm.username, 'Username*') }}
                                            {{ form_widget(professionalRegistrationForm.username, {'attr': {'class': 'uk-input'}}) }}
                                        </div>
                                        <div class="uk-margin">
                                            {{ form_label(professionalRegistrationForm.email, 'Email*') }}
                                            {{ form_widget(professionalRegistrationForm.email, {'attr': {'class': 'uk-input'}}) }}
                                        </div>
                                        <div class="uk-margin">
                                            {{ form_label(professionalRegistrationForm.password.first, 'Password*') }}
                                            {{ form_widget(professionalRegistrationForm.password.first, {'attr': {'class': 'uk-input'}}) }}
                                        </div>
                                        <div class="uk-margin">
                                            {{ form_label(professionalRegistrationForm.password.second, 'Confirm Password*') }}
                                            {{ form_widget(professionalRegistrationForm.password.second, {'attr': {'class': 'uk-input'}}) }}
                                        </div>
                                        <div class="uk-margin">
                                            <label><input class="uk-checkbox" type="checkbox"> I agree to the <a href="#">Privacy Policy</a> and <a href="#">User Agreement</a>.</label>
                                        </div>
                                        <div class="uk-margin">
                                            <button class="uk-button uk-button-primary uk-width-1-1">Register Account</button>
                                        </div>
                                    </fieldset>
                                    <input type="hidden" name="formType" value="professionalRegistrationForm">
                                {{ form_end(professionalRegistrationForm) }}
                            </div>
                            <div>
                                <p>Pintex makes it easy for educators to find community resources such as guest instructors, site visits hosts, mentors, advisors and project providers.</p>
                                <div class="uk-placeholder uk-text-center">This feature will be delivered August 9, 2019.</div>
                                 <h1>Educator Register</h1>
                                    {{ form_start(educatorRegistrationForm, {attr: {'novalidate' : 'novalidate'}}) }}
                                    {{ form_row(educatorRegistrationForm.firstName) }}
                                    {{ form_row(educatorRegistrationForm.lastName) }}
                                    {{ form_row(educatorRegistrationForm.email) }}
                                    {{ form_row(educatorRegistrationForm.username) }}
                                    {{ form_row(educatorRegistrationForm.password.first) }}
                                    {{ form_row(educatorRegistrationForm.password.second) }}
                                    <input type="hidden" name="formType" value="educatorRegistrationForm">
                                    <button class="btn">Register</button>
                                {{ form_end(educatorRegistrationForm) }}
                            </div>
                            <div>
                                <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Ad adipisci asperiores assumenda commodi deserunt dolor in maxime nemo perferendis praesentium, quam quia quisquam recusandae reprehenderit sint suscipit voluptate! Iste, nostrum?</p>
                                <div class="uk-placeholder uk-text-center">This feature will be delivered August 23, 2019.</div>
                                 <h1>Student Register</h1>
                                   {{ form_start(studentRegistrationForm, {attr: {'novalidate' : 'novalidate'}}) }}
                                   {{ form_row(studentRegistrationForm.firstName) }}
                                   {{ form_row(studentRegistrationForm.lastName) }}
                                   {{ form_row(studentRegistrationForm.email) }}
                                   {{ form_row(studentRegistrationForm.username) }}
                                   {{ form_row(studentRegistrationForm.password.first) }}
                                   {{ form_row(studentRegistrationForm.password.second) }}
                                   <input type="hidden" name="formType" value="studentRegistrationForm">
                                   <button class="btn">Register</button>
                                {{ form_end(studentRegistrationForm) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
